# SoundCloud Release Tracker

Windows standalone build of SoundCloud Release Tracker v4. The downloadable EXE is built automatically with GitHub Actions and does not require Python on the end user's PC.
